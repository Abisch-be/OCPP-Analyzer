services:
  ocpp-analyzer:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - ollama_net

networks:
  ollama_net:
    external: true
